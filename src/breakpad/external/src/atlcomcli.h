// HINT: this is a fake file which is not included if the system file exists
// The compiler will include this one only if the system file does not exist which means we are most probably on a MSVC Express edition and will activate a workaround
#define MSVC_EXPRESS_FIX