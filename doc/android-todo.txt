
- Escape HTML tags in chatbox on server side.

- 'Kick player' popup menu cannot be closed by clicking outside of the menu.

- MacOsX: all fonts are broken - possibly SDL_SetClipRect() fails.

- Prevent display surface from changing orientation when app is put to background.

- Prevent server from disclosing IP addresses of clients, because Kurko thinks it's privacy breach.

- 'Connecting to server' screen still freezes on Android until the screen is touched.

- PUSH notification service.

- Post to /r/androidgaming.

- Switch to mouse GUI navigation mode if mouse is moved.
